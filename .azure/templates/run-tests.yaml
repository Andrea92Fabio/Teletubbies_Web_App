steps:
# Test integration (si interrompono se falliscono)
- template: ../tests/integration-tests/api-tests.yaml

# Test E2E (si interrompono se falliscono)  
- template: ../tests/integration-tests/e2e-tests.yaml
